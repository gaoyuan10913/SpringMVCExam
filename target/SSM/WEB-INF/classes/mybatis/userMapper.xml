<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.hand.dao.UserDao">

	<cache eviction="FIFO" flushInterval="60000" size="500" readOnly="true"></cache>

	<insert id="save" parameterType="User" useGeneratedKeys="true" keyProperty="uId" flushCache="true">
		insert into sys_user(uName, uAge) values(#{uName}, #{uAge})
	</insert>
	
	<select id="selectById" parameterType="int" resultType="User" useCache="false">
	    select * from sys_user where uId=#{uId}
	</select>
	
	<delete id="deleteById" parameterType="int" flushCache="true">
	   delete from sys_user where uId=#{uId}
	</delete>
	
	<select id="selectAll" resultType="User">
	    select * from sys_user
	</select>
	
	<update id="updateById" parameterType="int">
		update sys_user set uName=#{uName} and uAge=#{uAge}
	</update>
	
</mapper> 
